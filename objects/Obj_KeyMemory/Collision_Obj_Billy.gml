/// @description Insert description here
// You can write your code in this editor
if left {
with(Obj_Key) {
 if x < instance_nearest(Obj_Billy.x,Obj_Billy.y,Obj_KeyMemory).x instance_destroy();
 }
}

if right {
with(Obj_Key) {
 if x < instance_nearest(Obj_Billy.x,Obj_Billy.y,Obj_KeyMemory).x instance_destroy();
 }
}